# 贡献规范

## 开发流程

1. 从主分支创建短生命周期功能分支。
2. 一次提交只解决一个明确问题；提交信息使用简洁中文动宾结构。
3. Controller 或 SQL 变化时同步运行文档生成器。
4. 按 [验证清单](docs/testing-and-troubleshooting.md) 执行与改动相关的构建、测试和静态检查。
5. 合并请求写明范围、配置变化、数据库影响、验证结果和回滚方式。

## 代码边界

- 外部请求经 8222 网关进入；Feign 内部接口使用 `/inner/` 前缀并限制公网访问。
- 关系表变更应提供可审查的增量迁移，禁止直接修改已在共享环境执行的脚本。
- 不提交 `target`、`node_modules`、`.nuxt`、IDE 文件、日志或本地证书。
- 不提交真实密钥、密码、token、手机号、证件号、订单或支付数据。
- 新配置优先使用环境变量，并在 `docs/configuration.md` 记录名称、用途和是否必需。

## 接口约定

- 新接口注明 HTTP 方法、路径、访问范围、请求模型和返回模型。
- 业务服务保持统一 `R` 响应约定；异常由全局异常处理转换，不在 Controller 吞掉关键上下文。
- 破坏性接口不得使用 GET；现有遗留路由逐步迁移时需要兼容与废弃说明。
