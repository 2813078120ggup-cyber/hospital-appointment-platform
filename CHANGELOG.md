# 变更记录

## 2026-08-26

- 用户中心新增修改账号信息与意见反馈：患者门户 `/user/account` 可修改昵称/姓名（service-user 新增 `POST /api/user/auth/updateUserInfo`）；`/user/feedback` 提交意见反馈，分平台反馈与医院反馈。
- 意见反馈接收：平台反馈由管理端 `/admin/hosp/feedback/*` 列表处理；医院反馈由医院端 `hospital-manage` 意见反馈页通过签名接口 `/api/hosp/feedback/list`、`/api/hosp/feedback/handle` 拉取与处理。反馈数据存于 `yygh_hosp.feedback`。

## 2026-08-26

- 医院端新增医生登录与排班管理：新增 `doctor` 账号表（从 `yygh_manage.schedule` 已存在医生自动生成账号），医生通过 `hospital-manage` `/doctor/login` 登录后查看本人排班，并可对排班执行停诊/恢复；平台 `service-hosp` 新增签名接口 `/api/hosp/schedule/suspend` 同步排班状态，停诊排班在患者端不可再约。

## 2026-08-26

- 完善项目总览与架构文档：明确医院系统、平台管理端和用户操作端边界，补充挂号支付退号业务时序及技术栈落地状态。
- 恢复患者门户首页医院等级和北京地区筛选：新增只读公共字典接口，保留管理端鉴权，并修正历史地区字典编码。
- 为本地演示配置管理端默认账号 `admin/123456`，保留环境变量覆盖，并明确生产环境必须使用强密码。
- 修正患者门户排班页医院导航的旧 `/hosp` 路径，统一跳转到医院主页、详情、预约须知和停诊信息页面。
- 完善短信登录验证码链路：发送成功后才写入 Redis，验证码 5 分钟有效、同手机号 60 秒内限发，并在登录时原子校验后一次性消费；移除固定验证码后门。
- 完善 AI 挂号工具：保留 Java 反射参数名，限制模型仅传入预约白名单字段，接通号源查询、正式订单创建、幂等重试和平台取消。
- AI 服务默认使用本机 Ollama 的 OpenAI 兼容接口，并生成可直接 `java -jar` 启动的可执行包；生产环境仍可通过环境变量切换模型供应商。
- 将会调用真实模型或直接修改 MySQL/MongoDB 的 AI 演示测试改为显式集成测试开关，默认构建不再依赖第三方密钥或污染本地数据。
- 医院模拟端改为读取当前医院配置中的编号，并修正排班同步成功后仍返回 `false` 的状态语义。
- 扩展正式订单迁移，为 AI 预约时段补齐可重复执行的唯一索引，阻止并发重复预约记录。
- 完成本地真实链路验证：AI 号源查询返回可用，正式订单创建后 MySQL/Mongo 号源各扣减一次，取消后订单关闭、AI 关联清理且两侧号源恢复。
- 将临时用于本机联调的后端基础设施、医院平台调用及 AI 挂号调用默认地址恢复为 `192.168.6.101`，RabbitMQ 恢复默认端口 `5672`，同时保留环境变量覆盖能力。
- 修复 Windows 多网卡环境的 Nacos 注册选址：所有微服务和网关显式注册 VMnet8 的 `192.168.6.1`，并支持通过 `YYGH_NACOS_DISCOVERY_IP` 覆盖。
- 完善患者门户订单与就诊人页面：统一登录、加载、失败和空数据状态，并恢复本地 mock 登录下的真实数据库列表展示。
- 完善平台管理端第一期运营能力：新增实时运营总览、预约订单查询与详情、用户启用和锁定操作，并修复医院上下线、字典导入导出、认证筛选及未来 7 天预约统计。
- 新增管理端公告运营中心：平台公告和停诊公告支持草稿、编辑、发布、下线、删除与分页筛选，患者门户首页改为读取真实已发布公告，并提供公告详情和全部列表弹窗。
- 完善管理端订单异常处置闭环：管理员可在退号时限内按原因取消预约，已支付订单联动退款，记录来源、操作人、结果与失败信息；运营总览及订单列表增加取消处理中和异常筛选。

## 2026-08-23

- 初始化“医院预约挂号平台”单仓库，纳入医院模拟端、微服务后端、运营管理端和用户门户。
- 新增系统架构、开发、配置、接口、数据库、验证排障和贡献文档。
- 根据初始化 SQL 生成 5 个数据库、11 张表的数据库字典与纯 DDL 脚本，排除包含个人信息风险的 INSERT 数据。
- 将 OSS、微信开放平台、微信支付和 JWT 敏感配置改为环境变量。
- 将医院模拟端 Lombok 锁定为 1.18.30，兼容当前 JDK 21 构建环境。
